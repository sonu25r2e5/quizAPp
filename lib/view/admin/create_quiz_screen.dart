import 'package:flutter/material.dart';

class CreateQuizScreen extends StatelessWidget {
  const new({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Create Quiz Widget')));
  }
}
